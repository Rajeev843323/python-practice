"""Python does not have built in support of arrays. but python list can be use as array.
to work with array in python import numpy library. An array is a special variable which
can hold more than one value as a time """

import numpy as np

